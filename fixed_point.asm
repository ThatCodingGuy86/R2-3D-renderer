; This is a library that implements fixed point operation macros.
; The fixed point operations implemented are: 
; Addition, subtraction, multiplication, division
; Others will be added when I need them in my own programs.

; Fixed point convention:
; The size of the type is 2 words (32 bits on the R2)
; 

%macro f_add

%endmacro

%macro f_sub

%endmacro

%macro f_mul

%endmacro

%macro f_div

%endmacro